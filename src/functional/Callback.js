import React from 'react'

const Callback = (props) => {
    
        return (
            <div>
                {console.log('callback props',props)}
                Loading...
            </div>
        )

}

export default Callback
